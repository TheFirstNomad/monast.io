# Public (client-side) backend config — safe to expose in the browser bundle.
# Real values are generated automatically by Lovable Cloud into .env.
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-anon-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"

# Server-only secrets are NEVER stored in this repo or in .env.
# They live in backend secrets and are read by edge functions at runtime:
#   CIRCLE_API_KEY, SUPABASE_SERVICE_ROLE_KEY, LOVABLE_API_KEY, ...

# Google Web Client ID for Circle native Social Login (public value).
# Paste the same client ID into Circle Console -> Wallets -> User Controlled ->
# Configurator -> Social Logins -> Google, and add https://monast.io/auth (plus
# your preview origin) to the credential's authorized redirect URIs/origins.
VITE_GOOGLE_CLIENT_ID="1032617147194-qjn7n0e1mm8d2213h0aprorcb4fg9ghm.apps.googleusercontent.com"
